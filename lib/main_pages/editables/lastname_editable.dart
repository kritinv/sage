import 'package:flutter/material.dart';

class LastNameEditable extends StatelessWidget {
  static const routeName = '/lastnameeditable';

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Container(child: TextField()));
  }
}
